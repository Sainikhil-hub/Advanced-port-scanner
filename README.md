# Advanced Port Scanner (Python)

A multi-threaded port scanner built using Python sockets.  
It scans a target IP for open ports and identifies running services.

## Features
- Multi-threaded fast scanning
- User-defined port range
- Service detection (HTTP, MySQL, etc.)
- Banner grabbing support
- Scan summary output

## Usage
```bash
python3 scanner.py
